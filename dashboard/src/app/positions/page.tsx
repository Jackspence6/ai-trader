import { Placeholder } from "@/components/placeholder";

export default function Page() {
  return (
    <Placeholder
      title="POSITIONS"
      spec="PER VENUE AND AGGREGATE"
      items={["Delta exposure by underlying — proves neutral strategies are neutral","Leverage and liquidation distance with escalating warnings","Accrued funding per position","Full order and fill audit trail with latency breakdown"]}
    />
  );
}
